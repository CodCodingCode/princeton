"""External-data clients (ClinicalTrials.gov, DGIdb, RCSB, …).

Kept sibling to `agent/` so pipeline modules and orchestrator steps can both
import the same fetcher without circular-dependency hell.
"""
